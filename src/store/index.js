import { createStore } from 'vuex'

export default createStore({
  state: {
  },
  getter:{
    //计数属性
  },
  mutations: {
    //唯一修改state的途径，同步操作
  },
  actions: {
    //进行异步操作，要传context（上下文）
  },
  modules: {
    //模块化开发
  }
})
